@model IEnumerable<WebApplication5.Models.Tblappointmentstatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Appstatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Appstatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Statusid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Statusid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Statusid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
